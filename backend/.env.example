# DATABASE_URL is automatically resolved to backend/internship.db.
# Only uncomment and change this if you are using an external database like PostgreSQL.
# DATABASE_URL=postgresql://user:password@localhost/dbname

BACKEND_PORT=8000
FRONTEND_PORT=5173
